name: CI

on:
  pull_request:
    branches: [ "main", "master" ]
  push:
    branches: [ "**" ]

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate Gradle Wrapper
        uses: gradle/wrapper-validation-action@v2
        continue-on-error: true

      - name: Set up JDK 17 (AGP runtime)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: 'gradle'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Make gradlew executable
        run: chmod +x gradlew

      # Builds all modules; your app has Java 11 target, which is fine.
      - name: Assemble Debug
        run: ./gradlew assembleDebug --stacktrace

      - name: Upload APKs
        uses: actions/upload-artifact@v4
        with:
          name: apks
          path: "**/*.apk"

  # TESTS DISABLED - Uncomment to re-enable
  # unit-tests:
  #   name: unit-tests
  #   runs-on: ubuntu-latest
  #   needs: build
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-java@v4
  #       with:
  #         distribution: temurin
  #         java-version: '17'
  #         cache: 'gradle'
  #     - uses: gradle/actions/setup-gradle@v3
  #     - run: chmod +x gradlew

  #     # Runs unit tests across modules/variants (Debug)
  #     - name: Run unit tests
  #       run: ./gradlew testDebugUnitTest --stacktrace

  #     - name: Upload unit test reports
  #       if: always()
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: unit-test-reports
  #         path: |
  #           **/build/reports/tests/testDebugUnitTest/**
  #           **/build/test-results/testDebugUnitTest/**

  # android-lint:
  #   name: android-lint
  #   runs-on: ubuntu-latest
  #   needs: build
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-java@v4
  #       with:
  #         distribution: temurin
  #         java-version: '17'
  #         cache: 'gradle'
  #     - uses: gradle/actions/setup-gradle@v3
  #     - run: chmod +x gradlew

  #     - name: Android Lint (all modules)
  #       run: ./gradlew lint --stacktrace

  #     - name: Upload lint reports
  #       if: always()
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: lint-reports
  #         path: "**/build/reports/lint-results*.*"
